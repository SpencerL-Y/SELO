#include <stdlib.h>

int main(){
    // int whatever;
    int * data = malloc(2*sizeof(int));
    // int * data2 = malloc(sizeof(int));  
    // int i = *(data + 1);
    // free(data);
    // int* j = NULL;
    // int* i = j;
    // // *(data + 1) = whatever;
    // int n = *(data+1);
    // if(n > 0) {
    //     free(data);
    // }
}
