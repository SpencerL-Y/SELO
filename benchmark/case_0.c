#include <stdlib.h>

int main(){
    // int whatever;
    int * data = malloc(sizeof(int));
    // // *(data + 1) = whatever;
    // int n = *data;
    // if(n > 0) {
    //     free(data);
    // }
}
